{
  "name": "importback",
  "version": "1.0.0",
  "description": "import backend : 최저가 탐지 서비스",
  "license": "ISC",
  "author": "umsehun",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "start": "uvicorn main:app --reload --port 8000",
    "test": "pytest tests/",
    "test:cov": "pytest tests/ --cov=src --cov-report=term-missing --cov-report=html",
    "type-check": "mypy .",
    "clean": "bash scripts/clean.sh"
  }
}
